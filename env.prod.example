# Production Environment Configuration
NODE_ENV=production

# Application
PORT=3000
API_PREFIX=api/v1

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=production_password_change_me
DB_NAME=personal_finance_prod

# JWT Configuration
JWT_SECRET=production-secret-key-change-in-production
JWT_EXPIRES_IN=1d

# Logging
LOG_LEVEL=error

# Swagger/OpenAPI (disabled by default in production)
SWAGGER_ENABLED=false
SWAGGER_PATH=docs
